# ログ（追記専用・新しい順）

1 行 = 1 節目（PR の merge・ADR・人手作業の完了・本番の状態変化）。`- YYYY-MM-DD 何を（#PR / ADR / skill）`。
自動ロードはされない。必要なら `head -20 docs/log.md`。作業中の試行錯誤は書かない（git log と PR にある）。

- 2026-09-05 #56 を merge・本番稼働 — 投稿フォームのダイアログ化（requirements 14）+ タイムラインの既定を「写真だけ」に（requirements 15 の前半）。記録フォームをページ上部の常駐カードから、「みんなの記録」の見出し横の「＋ 記録する」で開く `<dialog>`（showModal）へ（API・スキーマ・migration なし）。閉じ方は 閉じる / Esc / Android の戻る / 広い画面では背景タップ（`closedby="any"` + Safari 向けの `target === dialog` fallback — 拡大表示と同じ手。中身の form が dialog を満たすので余白のタップが背景扱いにならない）。決め: **閉じても入力は捨てない**（フォームは閉じた dialog の中に残り、開き直せば続きから。送れたら空にして閉じ一覧へ、写真だけ送れなかったときは開いたまま知らせる — 閉じると知らせを読む場が無い）、**サジェストは開いている間だけ読む**（`open && <SuggestionPicker/>`。ホームを眺めるだけの読み込みにリクエストを足さず、開くたびに読み直すので、記録の増減に追従させる再読込の配線 `mealsVersion` / `onRecordsChanged` を外せた — modal の間は記録が動かない）、**焦点は showModal の既定**（最初のフォーカス可能要素 = 閉じる）で料理名に autofocus しない（スマホでキーボードが立ってサジェストの札を隠す）、トリガーの表示は「＋ 記録する」（「たべたものを記録する」はスマホ幅で見出しの下に折り返し、写真が 1 行下がる）で読み上げ名は `aria-label` で補う。見た目はスマホが全画面のシート（見出しと閉じるは sticky。`inset: 0` + auto で fixed の箱を満たし、iOS のツールバーの出入りに追従）、40rem 以上は中央カード。modal の間は `html:has(dialog:modal) { overflow: hidden }`（ホイールで背後が流れる・スマホでスクロールが document へ抜けて引っ張り更新で下書きが消える、を防ぐ。拡大表示にも効く）+ `scrollbar-gutter: stable`。`modern-web-guidance` の light-dismiss-a-dialog / platform-controls-dismiss-dialog を参照（`closedby` は Chrome 134 / Firefox 141 のみで Safari 未対応 → fallback 必須）。unit 164 のまま（ロジック追加なし）、e2e は golden-path / link-preview を dialog を開く流れに直して 4 spec 8 passed（送れたら閉じる・閉じても入力が残る・焦点が開いたボタンへ戻る・Esc を足した。既定が写真だけなので reload / ホームに戻るのあとで くわしく を押し直す）。Playwright のスクリーンショット（390×844 / 1280×800）で全画面シートと中央カードを確認。本番 deploy は配信 asset がローカルビルドと一致することで確認
- 2026-09-05 要件を 2 件追加（ユーザ指示）— 14 投稿フォームのダイアログ化（トップの投稿ボタンから開き、フォームが常にページ上部を占めない）/ 15 写真ファーストのトップページ（トップは検索と投稿済み写真を主役に、タイムラインの既定を「写真だけ」に）。roadmap は Phase 3 に積み、Phase 4（D1 バックアップ・bot scan 対策・部分一致検索）はさらに後回しに
- 2026-09-05 #53 を merge・本番稼働 — 写真グリッドのタイムライン（requirements 13）。**これで Phase 3（振り返れる）の要件は全部本番**。「みんなの記録」に くわしく / 写真だけ の chips（ふりかえりと同じ流儀）を置き、写真だけは 1 投稿 1 セルの壁（API・スキーマ・migration なし。追加リクエストもなし — ホームが読んだ LIMIT 50 の同じ配列の描き替えだけで、グリッドに要る値は全部 MealSummary にある）。決め: **写真の無い投稿はセルを持たない**（絵のない枠は「眺めて楽しむ」の邪魔。その記録は くわしく にいる）、**並びは一覧のサーバー順のまま 1 投稿 = 1 セル**で代表はカルーセル先頭と同じ 1 枚目（同じ料理の別アングルで壁が埋まらず、1 セル = 1 品なので次に食べたいものを目で探せる）、日付見出しは置かない（いつのものかは拡大の中）、**件数はグリッド専用に深く遡る API を作らず一覧と同じ窓** — 2 つの見方が別の期間を映すと混乱するし、ページングが要件になれば共通の口で両方伸びる。**セルの飛び先は既存の拡大表示**（1 枚目から開き、中で送れて削除も従来どおり）で、グリッドでは料理名がどこにも無いので拡大に「料理名 + 食べた日」のキャプションを追加（一覧から開いても役立つ）。セルはサムネ 320px（`minmax(6rem, 1fr)` の auto-fill でスマホ 3 列 ≈ 110px、DPR 3 でほぼ等倍）。♥ と「まだある」印をセルの隅に — 印は inline SVG（文字の ⧉ はフォント次第で豆腐になる端末がある）。ついでに拡大表示の開き位置合わせを useEffect → **useLayoutEffect**: 送ってから閉じて別の投稿で開き直すと、paint 後の goTo が走るまで前回の位置の札が 1 フレーム描かれる（札 2 / 2 で写真は 1 枚目 — #51 からの潜在。スクリーンショットの取得タイミングで再現・解消を確認）。unit は photoGridItems（写真なしは出ない・並びは一覧のまま・代表は 1 枚目・1 投稿 1 セル）5 本を足して 164、e2e は 4 spec 8 passed のまま（既定表示は くわしく なので既存 spec は素通り。表示の変更に e2e は足さない — テスト方針）。ふりかえりへの展開は見送り（またたべたい × 写真だけ は魅力的だが絞り込み UI が既に 2 段ある節への 3 段目の置き方は別途。MealList が view prop を持ったので足すのは chips 数行）。本番 deploy は配信 asset がローカルビルドと一致することで確認
- 2026-09-04 #51 を merge・本番稼働 — 写真のカルーセル（requirements 12）。一覧の 5.5rem タイル + 送れない拡大表示を、カード幅いっぱいのカルーセルに置き換え（API・スキーマ・migration なし）。送りは**横スクロール + scroll-snap を土台に、← 2 / 3 → を写真の下**に置く二段構え — スマホは指のスワイプが第一級（慣性も端の跳ね返りもブラウザのものが一番よく、JS の drag より触る量が少ない）で、scroll-snap だけだとマウスと Safari のキーボードで送れないため。**いま何枚目かは `scrollLeft ÷ 幅` で数える** — `scrollsnapchange` / `scroll-initial-target` / `container-type: scroll-state` はどれも Chrome だけ（`modern-web-guidance` の Fallback strategies で確認）で、iPhone と Android の両方を使う家族には前提にできず、IntersectionObserver の fallback は「重い fallback を入れない」に反する。スライドを gap 0 の 100% 幅にして割り算 1 本で済ませた（`src/lib/carousel.ts`）。複数枚は正方形にそろえ（高さが跳ねると送るたびに下のタグ・メモが動く）、1 枚だけならそのままの縦横比で飾りも出さない（そろえる相手がいないのに切ると料理の端が理由もなく落ちる。画面を覆うほど縦長のものだけ 60dvh で止めて切る）。拡大表示の中でも送れ（指・← →・キーボードの矢印）、タップした 1 枚から `behavior: "instant"` で開く（開いた瞬間に送らない）。「写真を削除」はいま見えている 1 枚を消す。端の矢印は `disabled` ではなく `aria-disabled` — disabled にすると押した瞬間に焦点が body へ落ちて送る手が止まる（Playwright も aria-disabled を非 actionable と見るので、端で止まることが e2e から裏取りできた）。一覧の画像はサムネ 320px → 本体 1600px（カード幅いっぱいだとサムネは粗い。`loading="lazy"` なので読むのは見た分だけ、拡大時は同じ URL がキャッシュ済み。サムネは記録・編集フォームの小さな一覧が使い続ける）。unit は `snapIndex` / `clampIndex`（半分を越えたら次の 1 枚・iOS のゴムのような戻りの負値・まだ描かれていない幅 0）で 159 本、e2e は 4 spec 8 passed のまま（表示の変更に e2e は足さない — テスト方針）。本番 deploy は配信 asset がローカルビルドと一致することで確認
- 2026-09-04 #49 を merge・本番稼働 — 実装 PR ②（ADR-008 §4 §7-8）。投稿の編集 UI。一覧の各行に「編集」を足し、押すとその行が入力欄に変わる（`MealList` は みんなの記録 と ふりかえり の共通コンポーネントなので、どちらの一覧でも直せる）。欄は投稿フォームと共有する `MealFields` に切り出し、**id は `idPrefix` で分ける** — 同じページにフォームが 2 つ出るので `htmlFor` が衝突すると label が別のフォームの入力を指す。編集ではサジェストの札を出さない（引き継ぎは「新しく記録する」ための道具で、編集の場に置くと自分の記録を他の回の内容で上書きできる）。写真は「保存する」を待たずその場で足し引きする（meal が既にあるので作成時の「送信まで手元に貯める」概念が要らない）。保存後は行をサーバー応答で置き換え、食べた日が変われば日付見出しごと並べ直す（`sortByRecency` をホームの挿入と共有）。フォームを閉じたら「編集」ボタンへ焦点を戻す。unit は `mealFormFrom`（記録を欄に写す・null は空欄・欄 → body の往復で値が落ちない）と `sortByRecency` で 154 本。e2e は golden path に「編集で料理名・タグ・ひとことメモを直し写真を 1 枚足す → リロードで残り、♥・写真・リンク・作り方メモは動かない（PUT の body に無い＝別リソース）」を、link-preview に「レシピ URL を貼り替えるとカードが差し替わり旧 og:image は 404、触っていない お店・商品 の欄はそのまま」を足して 4 spec 8 passed（fixture に `swapped.html`）。本番 deploy は配信 asset がローカルビルドと一致することで確認
- 2026-09-04 #48 を merge・本番稼働 — 実装 PR ①（ADR-008 §1-6）。`PUT /api/spaces/:spaceId/meals/:mealId` で記録の内容を全置き換え。body は作成と同じスキーマ（`CreateMealInput` → `MealContentInput` に改名して共用）で、またたべたい（PATCH）と写真（子リソース）はこの口では動かない。応答は一覧と同じ形の 1 件（`getMeal` を `listMeals` から切り出して共有）。認可はスペースのみで `created_by` / `created_at` は動かさず `updated_at` だけ動く。タグは `meal_tags` を張り替え（差分を取らない。upsert → DELETE → `INSERT … SELECT` を作成と同じ 1 batch）。**凍結列 `recipe_source_type` は UPDATE でも毎回導出する** — 作り方メモを消す編集で `'text'` のまま残すと CHECK 違反で落ちる（ADR-007 §2 の凍結は書き込みごとの義務）。プレビューは `planLinkPreviews` で kind ごとに keep / replace / drop を決め、貼り替えた欄だけ行を捨てて取り直す（捨てる og:image は R2 が先）。取得中の編集に備えてジョブの UPDATE を `AND url = ?` で自分が取りに行った URL に限定し、og:image の R2 キーを取得ごとに固有（`ogp/<spaceId>/<mealId>/<kind>/<uuid>`）にした — キーが同じだと負けたジョブの補償削除が勝った側の画像を消す。プレビュー画像の配信だけ `Cache-Control: private, no-cache`（URL は kind ごとに固定で中身が貼り替わるため。写真は id ごとに URL が一意なので `max-age` のまま）。migration なし、unit 149、e2e の boundary に PUT の横流れ 404 を追加
- 2026-09-04 ADR-008 を起草・merge（#47）— 投稿の編集（requirements 11）。**ADR-003 §5「編集 API は持たない（修正は削除 → 再投稿）」を撤回**（打ち間違い 1 文字のために写真・またたべたい・記録した日時・プレビューを捨てるのは製品意図 1 に反する）。編集は部分更新ではなく内容の全置き換え（`PUT`。PATCH は「キーが無い」と「null」の 2 種類の不在を持ち込む）、認可はスペースのみでメンバーなら誰の記録でも直せる（削除・またたべたいと同じ線）、タグは張り替え、写真は既存の子リソース API のまま（ADR-004 §6 が送った投稿後の写真 UI を回収）、URL を貼り替えたときだけプレビューを取り直す（ADR-007 §5 の「再取得の導線は持たない」を更新）、UI は一覧の行をその場でフォームに。migration なし。ADR-007 に残っていた「ADR-008 候補」（レシピ本文の取り込み）の番号予約も、記録どおり「番号は起草時に採る」へ直した
- 2026-09-03 要件を 3 件追加（ユーザ指示）— 11 投稿の編集 / 12 写真のカルーセル / 13 写真グリッドのタイムライン。roadmap は Phase 3 を再開してこの 3 件を積み、Phase 4（D1 バックアップ・bot scan 対策・部分一致検索）は後回しに。レシピ本文取り込みの ADR 番号予約（008）は外し、起草時に採る形へ
- 2026-09-03 #44 を merge・本番稼働 — 週 / 月の集計プリセットとタグクラウド（ADR-006 改訂 §5-8）。roadmap Phase 3 はこれで完了。期間は `StatsPeriod`（all / week / month / custom）の DU で持ち、from / to は `periodRange(period, today)` で毎回導出する（`src/lib/stats-period.ts` の純粋関数、unit 15 本）。offset は 0 が「いま」で負が過去、`stepPeriod` は 0 を超えないので未来へ進めない。日付演算は `new Date(y, m, d + n)` の繰り上がりに任せ、月末・年またぎ・閏日を自前で数えない。週の始まりは日曜（`Intl` の weekInfo は使わない）。集計 API `/meals/stats` は無改造で、クラウドが札の大きさに要する回数のためだけに兄弟 `GET /meals/tag-stats?from&to` を 1 本足した（`MealStatsQuery` 共用、`meal_tags ⋈ tags ⋈ meals` を `meals.space_id` と期間で絞り `GROUP BY tags.id`、多い順 → `name_normalized` 昇順・上限 40。表示名は group by しているタグ行の列なので bare column の非決定は起きない。`GET /tags` は語彙の口のまま無改造）。migration なし。集計の既定は全期間 → 今月（一覧の既定 またたべたい は据え置き）。クラウドの札は「その食材の記録をさがす」入口で、♥ の枠を外して 1 タグに絞り、section へ focus を移してからスクロールする（`scroll-behavior` は `prefers-reduced-motion` で切る）。札の大きさは回数の平方根だが、回数に差が無ければ大きさも変えない（無い強弱を大きさで語らない）。region 名は「よく食べているもの」→「食べたもののまとめ」で、中の h3 2 つが同じ期間を見る。e2e は golden path の集計ブロックを差し替え（既定の今月 → ← で先月は 0 件 → → で戻る → 日付指定の口 → クラウドから一覧へ）+ boundary に `/meals/tag-stats` の 404 と space 隔離を追加して 4 spec 8 passed、unit 141。本番 deploy は配信 asset がローカルビルドと一致することと、`/meals/tag-stats` が未認証で 401 + JSON（SPA へ落ちない）ことで確認
- 2026-09-03 #41 を merge・本番稼働 — 実装 PR ②（ADR-007 §3-7）。URL プレビューを投稿時スナップショットで持つ。migration 0005 は子表 `meal_link_previews`（PK `(meal_id, kind)`・CASCADE・kind / status に CHECK）の `CREATE TABLE` だけの additive で、`meals` を触らないので rebuild も CASCADE 事故も無い（backup なし）。取得は POST /meals の応答後（`waitUntil`）に走り、`HTMLRewriter` が og:*（property / name 両方）と `<head>` の `<title>` を拾う。ガードは 5 秒（body 読み出しまで）/ 512 KB / redirect 3 ホップ（各ホップで http(s) 確認）/ `text/html` 以外は読まずに中止 / UA `MatatabetaiBot/1.0` と名乗る。og:image は hotlink せず magic bytes + 5 MB で取り込んで private R2（`PHOTOS_BUCKET` の `ogp/<spaceId>/<mealId>/<kind>`。binding は増やさない）へ置き、配信は写真と重複していた R2 応答を `lib/r2-object.ts` に共通化した認可つき proxy。表示は `ok` のときだけカードを重ね、`pending` / `failed` / 行なしはプレーンリンク（投稿直後の自動再読込は持たない＝カードは次に一覧を読んだときに出る）。純粋関数（候補→保存値・行→表示 DU・相対 URL 解決）は `worker/domain/link-preview.ts` に切って unit 14 本、e2e は `e2e/fixtures/site/` を `dist/client/e2e-fixture/` に写して wrangler dev 自身に「外部サイト」を配らせ 4 spec 8 passed（og:title カード + R2 経由の画像デコード・`<title>` fallback・取れない URL はプレーンリンク・削除で R2 も 404・他スペースからの画像 404）。本番 deploy は配信 asset がローカルビルドと一致することで確認
- 2026-09-03 #39 を merge・本番稼働 — 実装 PR ①（ADR-007 §1-2）。レシピ・リンク欄を排他の `RecipeSource` DU から `recipeUrl` / `shopUrl` / `recipeMemo`（列は `recipe_text` を再利用）の独立 3 項目へ。migration 0004 は additive（`ADD COLUMN` 2 本 + 旧 `url` を一律 `recipe_url` へ backfill、rebuild なし・CHECK `meals_recipe_source_check` は凍結・backup なし）で、生成 SQL とスナップショット差分に `__new_*` が出ないこと・適用後も CHECK が効くことをローカル D1 で確認。凍結列は `frozenRecipeColumns`（作り方メモの有無で `text` / `none`、`url` は常に NULL）を導出して書き続ける。フォームは fieldset「レシピ・リンク」に 3 欄（`note` は「ひとことメモ」に改称）、一覧は種別つき `<a>` 2 本 + `<details>` の作り方メモ（PR ② のカードはこの `<a>` に重ねる）。unit は凍結 CHECK と同じ述語で導出を固定、e2e は golden path にリンク 2 種・作り方メモの入力とリロード後の残存・引き継ぎを足して 7 passed。本番 deploy は配信 asset がローカルビルドとバイト一致することで確認
- 2026-09-03 ADR-007 を起草・merge（#37）— レシピ・リンク欄を排他の `RecipeSource` から `recipe_url` / `shop_url` / `recipe_text`（作り方メモ）の独立 3 項目へ（併用可、`note` は別で引き継がない）。URL プレビューは投稿時スナップショット（`waitUntil` + 新表 `meal_link_previews` + og:image を private R2、失敗はプレーンリンク）。migration は additive（CHECK 凍結で rebuild の CASCADE 回避、旧 `url` は一律 `recipe_url` に backfill）。requirements 3〜5・8 改訂 + 10 追加・roadmap・CLAUDE.md 例（`RecipeSource` → `LinkPreview`）も同時改訂。本文抽出（JSON-LD `schema.org/Recipe` 路線）は規約調査を先行して ADR-008 へ分離。実装 PR ①② は backup を取らない方針（データ少・additive、ユーザー決定）
- 2026-09-02 #35（+#36）を merge — `block-main-commit` hook を Python 化し、`cd` / `git -C` を追って**その git が走るリポジトリ**のブランチを見る（skill 還元の `cd ../okayus-skills && git commit` を止めない）。判定表のテストを #36 で追加。`.claude/**` なので人間 merge
- 2026-09-02 Workers Builds へ移行完了（#33）— GitHub 側の Cloudflare credential がゼロに。既存 Worker があると「アプリケーションを作成する」経路はプロジェクト名で弾かれるため、Worker → 設定 → ビルド → Git リポジトリ → 接続で繋いだ（ルート `packages/web`、プレビュービルド OFF、監視パス除外 `docs/*` `*.md`、トークン `matatabetai Workers Builds`）。この経路は接続時にビルドが走らず、`deploy.yml` 削除の merge がそのまま初回 push 起動ビルドになり green・`/health` 200。secret 2 本と旧デプロイトークンを削除し、`plans/host-setup.md` を削除。skill `cloudflare-workers-builds-keyless-deploy` 0.4.0 を還元（okayus-skills #39）
- 2026-09-02 #31 を merge — Phase 3 第一弾（ふりかえりページ /lookback: またたべたい一覧・料理名の期間集計・タグ検索 AND、ADR-006）。migration なしで `GET /meals` にフィルタ（`?tags` AND・`?mataTabetai=1`）を足し、`/meals/stats?from&to` は `row_number()` rank=1 から表示名を拾って COUNT DESC・♥ は印（並びは変えない）。一覧描画を `MealList` / `TagFilter` へ共有化。e2e は golden path にふりかえり、boundary に stats 404 と集約の space 隔離（seed の他家族 meal に ♥ とタグ）を追加し 7 passed。本番 deploy 成功を `/health` で確認
- 2026-09-02 #29 を merge — 投稿時サジェスト（料理名ごとの直近 1 件を `row_number()` で名指し、タグ AND 絞り込み、前回の URL / レシピ / タグの引き継ぎ、ADR-005）。表を増やさず migration なし、絞り込み語彙に `GET /tags` を新設。投稿フォームを制御化し変換を `src/lib/meal-form.ts` の純粋関数へ。e2e は golden path に AND 絞り込みと引き継ぎ、boundary に集約クエリの space 越え検査を追加。本番 deploy 成功を `/health` で確認し **Phase 2（MVP）完了**。skill `cloudflare-workers-e2e-playwright` へ 0.3.0 を還元（okayus-skills はホスト未 commit）
- 2026-09-01 #27 を merge — 写真アップロード（meal_photos の migration 0003 が本番 D1 に適用、private R2 `matatabetai-photos` + Worker proxy 配信・クライアント縮小 1600px/320px・`<dialog>` lightbox、ADR-004）。e2e は golden path に upload → 配信 → 304 → 削除、boundary に写真ルートの横流れ 404 を追加。スマホからの写真付き投稿を実機確認し、Phase 2 の写真が本番稼働。skill `cloudflare-r2-private-image-upload` へ 0.2.0 を還元（okayus-skills はホストで commit 済み）
- 2026-09-01 R2 bucket `matatabetai-photos` の作成と `CLOUDFLARE_API_TOKEN` への R2 Edit 追加が完了（人手、host-setup 5 を削除）。残る人手作業は Workers Builds 接続のみ
- 2026-09-01 初回 owner 登録が完了（人手、host-setup 6 を削除）— owner のパスキー + 2 台目の計 2 台。残る人手作業は Workers Builds 接続と R2 bucket
- 2026-09-01 #22 を merge — Phase 2 記録機能の第一弾（meals / tags / meal_tags / またたべたい、migration 0002 が本番 D1 に適用、ADR-003）。投稿・一覧 UI、e2e は golden path に投稿・boundary に meal id 横流れ 404 を追加。タグ upsert → meal → meal_tags は 1 つの d1.batch
- 2026-09-01 ドメインは workers.dev のままで確定（決めること 4、#23）— RP_ID/ORIGIN 固定済みで作業なし。初回 owner 登録のゲートが外れた
- 2026-08-31 okayus-skills への還元（passkey-auth 0.2.0 / space-membership-invite 0.2.0 / e2e-playwright 0.2.1 / docker-sandbox 0.6.1）をホストで commit（人手）。#20 で host-setup 6 を初回 owner 登録だけに、ホストの wrangler は `packages/web/node_modules/.bin/wrangler` に統一
- 2026-08-30 #19 を merge — passkey 認証・スペース・招待リンク（migration 0001 が本番 D1 に適用、ADR-002）。ホストで `SESSION_SECRET` を投入し `/api/auth/login/begin` が 200。本番は登録の扉が閉じた状態（`registration_closed`）で稼働。skill 還元: passkey-auth 0.2.0 / space-membership-invite 0.2.0 / e2e-playwright 0.2.1 / docker-sandbox 0.6.1。ホストの `pnpm exec wrangler` は pnpm 11 の自動 install がコンテナの node_modules を消そうとして失敗 → `packages/web/node_modules/.bin/wrangler` を直接叩く運用に
- 2026-08-30 fine-grained PAT で `gh pr merge --auto --squash` の arm（GraphQL mutation）が通ることをコンテナから実証（#16 は CI 先行で即時経路、#17 で `enabledAt` → `ci` 待ち → merge）。okayus-skills token skill の Still open を閉じ 0.2.6 へ。同じ運用（opt-in merge + 安定シェル CI）を kokemusu / mazuoboeru にも展開し、既存リポの上げ方チェックリストを skill 0.2.5 に追加
- 2026-08-29 `protect-main` / `no-force-push-anywhere` ruleset を作成（active、bypass なし）— main の保護がサーバー側へ移り、public 化（08-23）が完結。#13 / #14 を merge、本番は最新 main。ruleset 無しでは `gh pr merge --auto` が CI を待たない（即時 merge / mutation 拒否）ことを実測し okayus-skills token skill 0.2.3 に還元（plans/host-setup.md 1 を削除）
- 2026-08-24 調査で「リポは 2026-08-23 に public 化済みなのに ruleset 未作成」と発覚 → 作成手順を plans/host-setup.md 1 に集約、ci.yml を安定シェル化（`.node-version` / root `ci` script / Dependabot、#13）、merge を `gh pr merge --auto --squash` の opt-in へ（例外は CLAUDE.md。ADR-001 改訂 2026-08-24、#14）
- 2026-08-24 deploy.yml の pnpm 衝突を修正（`pnpm/action-setup` の version 指定を削除し v6 へ、両 workflow を checkout/setup-node v7 + node 24 に）。本番が #10 のツールチェーンに追随（#12。plans/host-setup.md 0 を削除）
- 2026-08-24 ツールチェーンを wrangler 4 / vite 8 / `@cloudflare/vite-plugin` 1.x / pnpm 11 / TypeScript 7 へ更新、バインディング型を `wrangler types` 生成の global `Env` に移行（生成物は gitignore）、`compatibility_date` を 2026-08-20 へ（2026-08-04 以降は `nodejs_compat` / `nodejs_compat_v2` が既定 ON）（#10）
- 2026-08-24 #10 merge 後の Deploy が失敗（`pnpm/action-setup@v4` の `version: 9.15.0` と `packageManager: pnpm@11.22.0` の衝突）。**本番は #10 前のビルドのまま**で main と乖離。修正は `.github/workflows/**` = ホストで人手（plans/host-setup.md 0）
- 2026-08-23 ホスト側の準備が 2 つ完了: 1Password → `./shell.sh` の PAT 注入と、コンテナ内 claude の認証 + MCP 承認。以後 #8 / #9 / #10 はコンテナから push / PR できている（plans/host-setup.md の 2・3 を削除）
- 2026-08-23 進捗管理を status hub 化: `docs/status.md`（40 行上限）+ `docs/log.md` + SessionStart hook + `/handoff` + CI の上限検査、secrets 不要の `ci.yml`、`docs/roadmap.md` / `docs/plans/host-setup.md`。PROGRESS.md を廃止、CLAUDE.md を規約のみに（#7、skill `agent-status-hub`）
- 2026-08-23 push / PR 経路を 1 リポ限定 PAT（1Password 注入）に確定。注入点は同日中に `./up.sh`（コンテナ env）→ `./shell.sh`（exec 時のシェル）へ改訂＝ADR-001、docs アクセス 3 層（context7 MCP・docs egress）、`modern-web-guidance` 同梱。relay 下準備は撤去（#6、ADR-001 改訂）
- 2026-08-22 サンドボックス（node 24、Playwright 1.62.1、host port 5573）・docs・ADR-001 を整備、`RP_ID` / `ORIGIN` を shiraoka ホストへ（#4、#5）。okayus-skills v0.4.0 に passkey / space-invite / R2 の 3 skill を新設（本リポが最初の利用者）
- 2026-06 Cloudflare account subdomain 改名（`toshiaki-mukai-9981` → `shiraoka`）。本番 URL は `https://matatabetai.shiraoka.workers.dev`
- 2026-05-21 Walking Skeleton 本番稼働（#2）、`RP_ID` / `ORIGIN` を本番ホストに固定（#3）。D1 `matatabetai` 作成、空 migration 適用
- 2026-05-11 リポジトリ初期化・CLAUDE.md・main 直 commit 防止 hook（#1）
